name_keys: ['foo', 'uniform', 'gauss', 'int_seed']

defaults:
  bar: DEFAULT_BAR

matrix:
  - foo: ['barA', 'barB']
    single_val: 42
    uniform:
      n_samples: 2
      dist: uniform
      low: 0.8
      high: 1.0
    gauss:
      n_samples: 2
      dist: normal
      loc: 1.0
      scale: 2.0
    int_seed:
      n_samples: 1
      dist: uniformint
      low: 1
      high: 10
